\begin{figure}[!htbp]
  \centering
  \begin{tikzpicture}[
      >=Latex,
      every node/.style={font=\scriptsize,align=center},
      root/.style={draw,rounded corners,minimum width=.72\linewidth,
        minimum height=8mm,fill=black!4},
      variant/.style={draw,rounded corners,text width=.25\linewidth,
        minimum height=21mm,inner sep=3pt},
      conclusion/.style={draw,rounded corners,text width=.78\linewidth,
        minimum height=10mm,inner sep=3pt,fill=black!4}
    ]
    \node[root] (goal) {Party-level batch identity requirement};
    \node[variant,below=10mm of goal] (message)
      {\textbf{Message-level restriction}\\$m_1\neq m_2$\\
       Scoped injectivity holds;\\
       same-party batch reachable\\with different messages};
    \node[variant,left=4mm of message] (relaxed)
      {\textbf{Relaxed admission}\\No coordinate restriction\\
       Duplicate acceptance reachable};
    \node[variant,right=4mm of message] (party)
      {\textbf{Party-level admission}\\$A_1\neq A_2$\\
       Party distinction verified;\\valid batch reachable};
    \node[conclusion,below=9mm of message] (distinction)
      {Message distinction and scoped occurrence injectivity\\
       do not imply same-batch party distinction};
    \draw[->] (goal) -- (relaxed);
    \draw[->] (goal) -- (message);
    \draw[->] (goal) -- (party);
    \draw[->] (relaxed) -- (distinction);
    \draw[->] (message) -- (distinction);
    \draw[->] (party) -- (distinction);
  \end{tikzpicture}
  \caption{Controlled comparison of the three admission variants.}
  \label{fig:identity-control-comparison}
\end{figure}
